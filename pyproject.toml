[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "azzamal-diffusion2D"
description = "Diffusion equations"
readme = "README.md"
version = "0.0.1"
requires-python = ">=3.6"
keywords = ["diffusion"]
license = {text = "BSD License"}
dependencies = ["matplotlib", "numpy"]
[project.urls]
homepage = "https://github.com/Simulation-Software-Engineering/diffusion2d"