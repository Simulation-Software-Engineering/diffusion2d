[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "bergdola_diffusion2D"
description = "Solving the two-dimensional diffusion equation"
readme = "README.md"
version = "0.0.1"
requires-python = ">=3.6"
keywords = ["diffusion", "two-dimensional"]
dependencies = ["numpy", "matplotlib"]
